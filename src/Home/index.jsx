import React from "react"

function Home({ result }) {
    const { data } = result
    // console.log(data)
    return (
        <div>
            <h1>Home</h1>
        </div>
    )
}

export default Home
